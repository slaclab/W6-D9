{% include '@components/overlay-link/overlay-link.twig' with {
  image: content.field_media_image|field_value,
  link: content.field_boolean['#items'].value == 0 ? url : null,
  title: content.field_long_text|field_value is not empty ? content.field_long_text|field_value : content.name|field_value,
  kicker: false,
  label: false,
} %}

{% set catch_cache = content|render %}
